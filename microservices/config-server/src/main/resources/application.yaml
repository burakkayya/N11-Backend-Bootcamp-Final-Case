spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/burakkayya/N11-Config-Server
server:
  port: 8888